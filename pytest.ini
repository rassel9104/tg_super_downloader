[pytest]
testpaths = tests
asyncio_mode = auto
# Fuerza tmp y cache locales al repo (evita permisos en AppData/OneDrive)
addopts = -q -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
